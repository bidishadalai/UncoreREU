# UncoreREU environment requirements
# Regenerated from the working Jetstream env (`pip freeze`) on 2026-07-15 so every
# clone installs the same libraries. The previous pins (transformers 4.46, trl 0.12,
# etc.) no longer matched the scripts: clean_baseline_sst2.py uses SFTConfig(max_length=...)
# (newer trl), so installing the old pins produced errors like
# `SFTConfig got an unexpected keyword argument 'max_length'`.
# If you bump a pin, re-verify the training, eval, and pruning scripts still run.
#
# Install torch via the PyTorch index (match your CUDA), not plain PyPI, to avoid a
# version mismatch:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121

torch==2.12.0

transformers==5.10.1
peft==0.19.1
trl==1.7.0
accelerate==1.13.0

datasets==5.0.0
evaluate==0.4.6

tqdm

# Only needed for scripts/pruning-pipeline/pipeline.py.
llmcompressor==0.12.0
